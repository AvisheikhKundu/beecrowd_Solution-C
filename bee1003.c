#include <stdio.h>
    int main()
    {
         int x,y;
      
         scanf("%d %d", &x, &y);
         scanf("%d", &y);
         printf("SOMA = %d\n", x + y);
        return 0;
    }              
